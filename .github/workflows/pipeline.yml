name: Engine Predictive Maintenance pipeline

on:
  workflow_dispatch:

jobs:

  register-dataset:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r epm/requirements.txt
      - name: Upload Dataset to Hugging Face Hub
        env:
          HF_TOKEN_EPM: ${{ secrets.HF_TOKEN_EPM }}
        run: python epm/model_building/data_register.py

  data-prep:
    needs: register-dataset
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r epm/requirements.txt
      - name: Run Data Preparation
        env:
          HF_TOKEN_EPM: ${{ secrets.HF_TOKEN_EPM }}
        run: python epm/model_building/prep.py


  model-training:
    needs: data-prep
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r epm/requirements.txt
      - name: Model Building
        env:
          HF_TOKEN_EPM: ${{ secrets.HF_TOKEN_EPM }}
        run: python epm/model_building/train.py


  deploy-hosting:
    runs-on: ubuntu-latest
    needs: [model-training,data-prep,register-dataset]
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r epm/requirements.txt
      - name: Push files to Frontend Hugging Face Space
        env:
          HF_TOKEN_EPM: ${{ secrets.HF_TOKEN_EPM }}
        run: python epm/hosting/hosting.py
